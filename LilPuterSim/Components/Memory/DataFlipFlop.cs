namespace LilPuter
{
	public class DataFlipFlop
	{
		//Big fat todo!
	
		//A one bit register can be made with a Mux and a DFF, but it feeds the output back into the mux input that's selected when load is off.
		//We can handle feedback loops if I contain them in a system
		////the goal of the flipflop is that the dependency will be such that it reads its own output before then updating that output.
		///
		/// Anyway I don't want to deal with that right now.
	}
}